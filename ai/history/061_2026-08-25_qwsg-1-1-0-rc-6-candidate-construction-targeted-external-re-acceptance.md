# Task History 061: Approved Task

## Task metadata

- Task ID: `061`
- Task slug: `qwsg-1-1-0-rc-6-candidate-construction-targeted-external-re-acceptance`
- Status: `complete`
- Date generated: `2026-08-25` UTC
- Human authority: Project Owner
- Preferred owner communication language: English
- Related prompt: `ai/prompts/061_CURRENT_TASK.md`

## Lifecycle state

The Engineering Task Builder generated and transactionally installed this matching prompt/history pair from validated structured Owner input. The approved task constructed and froze RC.6, completed the Owner-bounded targeted external run, reconciled the product contract, and closed from the evidence scope explicitly terminated by the Project Owner.

## Starting state

- Started on 2026-08-25 UTC as ordinary user `attila` from the canonical QWSG
  repository root on `main`.
- Fetched `HEAD`, `origin/main` and direct Forgejo `refs/heads/main` matched
  `25a30718bc92882e9773a5c405ad648c0eee1a81`; ahead/behind was `0/0`, index
  and tracked worktree were clean, Framework 1.1.0 was valid, and
  `VERSION=1.1.0-rc.6`.
- The one active approved Task 061 prompt/history pair was the expected
  post-Builder state. The only unrelated path was the excluded Owner blueprint,
  verified by pathname metadata only and left untouched.
- No rc.6 archive, sidecar, tag, Forgejo Release, publication or Task 062
  existed. Task 059/RC.5 FAIL identities and Task 060 correction/closure
  identities matched immutable protected evidence.
- Baseline Framework (24), Builder (44), lifecycle (29), diversion (36),
  test-task audit, release plumbing, full Go, race, vet, formatting,
  deterministic ordinary-build contract, shell syntax and systemd static
  validation passed.

## Snapshot

- Private execution snapshot: `/tmp/qwsg-task061-execution.Y1bOdd`, mode
  `0700`.
- It contains a readable hashed tracked-HEAD archive, Task 061 lifecycle
  before-images, Git/ref/status/version evidence, immutable Task 059/060/RC.5
  hashes, release-workflow hashes, candidate/evidence absence claims and exact
  bounded rollback instructions. Owner content, credentials, private
  infrastructure identity, candidate bytes, caches and unrelated files are
  excluded.

## Work performed

- Chose canonical source epoch `1787698860` from the exact source commit,
  corresponding to `2026-08-25T23:01:00Z`.
- Constructed two isolated release outputs from separate exact `git archive`
  exports using the canonical `scripts/build-release.sh`, distinct private
  sources, outputs and Go caches. Archive and sidecar bytes compare equal.
- The first harness invocation ran the extracted script from the repository
  working directory and failed before output because Go correctly rejected an
  absolute package outside the active module. Running the script from its
  extracted source root corrected the harness. The aborted original loop had
  not created twin two, so twin one was retained unchanged and one missing
  isolated twin-two export/build was performed. No selected candidate existed
  during these recoverable orchestration corrections.
- Selected twin one exactly once and froze its archive and sidecar mode `0400`
  inside a private mode-`0700` build root. Candidate bytes have not changed
  since selection.
- Frozen candidate identity:
  - archive `qwsg-1.1.0-rc.6-linux-amd64.tar.gz`, size `2951540` bytes;
  - archive SHA-256
    `31161ad30bbb83b76e99744512355872dd765700bbe428a1ba0b02d157a7b567`;
  - sidecar SHA-256
    `a12869185065aaa070dba3a5577a8536b7bc5228bdeabe0f81218e775c6ef375`;
  - `MANIFEST.sha256` SHA-256
    `d1b9fb1e24b2b4c8053f8efb2188a803db99b7b4ab98f6d505a0210de70ee7c9`;
  - binary SHA-256
    `dcfd2f30dedfaf67dadafc86c04635a9fe9c4ec0bd801711f2da1a0ae1182b5a`;
  - source commit `25a30718bc92882e9773a5c405ad648c0eee1a81`, epoch
    `1787698860`, build time `2026-08-25T23:01:00Z`.
- Verified sidecar, safe unique 25-member single-root layout, only regular
  files/directories, 18 manifest entries, all manifest hashes, required
  LICENSE/README/INSTALL/docs/rc.6 notes, deterministic safe archive mode
  classes, binary version/provenance and absence of ambient Go VCS metadata.
  Two mode-check harness assumptions were rejected without candidate mutation:
  private extraction narrowed copy modes, so authoritative tar headers were
  used; the build time was then derived directly from the epoch instead of a
  manual conversion.

## Verification

- Source/release baseline: PASS.
- Deterministic archive equality: PASS.
- Deterministic sidecar equality: PASS.
- Frozen candidate integrity/layout/manifest/binary/docs/modes/provenance: PASS.
- External read-only VPS baseline: PASS. Privacy-safe Owner evidence confirms
  Ubuntu 24.04 amd64 ordinary-user context, exact installed RC.5 binary and
  packaged unit, enabled/active service, lingering, safe configuration/state,
  protected credential storage, and rc.6 absence. Notification preflight
  reports configuration/credential satisfied with transport verification
  correctly pending. RC.5 readiness remains exit `4`, canonical evidence
  missing-required, Guardian not-ready and overall not-ready, exactly
  preserving QWSG-059-F001 without mutation.
- Frozen candidate transfer and practical acceptance: pending Owner-side
  private access under the same Task 061 authorization. Owner-side destination
  verification passed for exactly two regular non-symlink files: archive size
  `2951540`, archive SHA-256
  `31161ad30bbb83b76e99744512355872dd765700bbe428a1ba0b02d157a7b567`,
  sidecar SHA-256
  `a12869185065aaa070dba3a5577a8536b7bc5228bdeabe0f81218e775c6ef375`,
  and sidecar check PASS. No extraction or installation was included in that
  evidence.
- External archive inspection then passed before installation: the archive
  retained its safe unique 25-member single-root layout with only regular
  files/directories. All 18 manifest entries verified, the extracted manifest
  and binary matched the frozen identities, and required LICENSE, README,
  INSTALL and rc.6 release notes were present. The candidate Smart Install
  check reported Ubuntu 24.04 amd64, glibc 2.39, ordinary-user execution,
  running user manager, systemd 255, `filesystem.local_semantics=satisfied`,
  environment ready and installation ready. Invoking the candidate binary
  without a subcommand opened its read-only Operator Console over the existing
  degraded/partial RC.5 state; it did not modify or conceal that baseline.
- The documented replacement stopped the exact enabled/active RC.5 user unit,
  retained effective configuration identity
  `sha256:a337c73f36345fbf9efd31763f3469e98a8e8e27e36339a6b069b844ae48763e`,
  and installed RC.6 with a new mode-0700 private backup. The first ordinary-
  user backup-content verification correctly encountered permission denial
  because the privileged installer created the private backup as root; this
  ended that `set -e` SSH shell without reboot or runtime repair. It is a
  bounded evidence-command issue pending privilege-matched read-only retest.
  RC.6 then verified as source commit
  `25a30718bc92882e9773a5c405ad648c0eee1a81`, controlled build time
  `2026-08-25T23:01:00Z`, exact binary and packaged unit, with configuration
  identity unchanged and the prior enabled/active service intent restored.
- All installed RC.6 release-owned artifacts compared byte-for-byte with the
  frozen package. Configuration/state roots and path components remained real
  non-symlinks with ordinary-user ownership and modes 0700/0600/0700, and
  configuration/state separation passed. Without restart or manual state
  repair, RC.6 readiness reported canonical evidence satisfied, Guardian
  enabled/active/ready, lingering satisfied and exit `0`; overall remained
  truthfully partial only because `notification.external` was still
  `unknown_requires_verification`. This is positive pre-scenario convergence
  evidence, not yet the required post-reboot correction proof.
- A second aggregate backup-verification command exited before its receipt;
  the root-owned backup remains untouched and requires four separately
  reported read-only facts to locate the failed expectation.
- Privilege-matched diagnosis found backup mode 0700, zero symlinks, 15 files,
  and the exact RC.5 binary SHA-256. The prior expectation of 16 backups did
  not account for RC.6's version-specific release-notes destination having no
  same-path RC.5 predecessor; that explanation and the prior notes' installed
  disposition remain pending a focused read-only check.
- Focused verification confirmed 15 is the correct replacement-backup count:
  RC.6's version-specific release notes were a new destination and therefore
  had no prior file to back up. RC.6 notes are installed; the unchanged RC.5
  version-specific notes remain present as legacy documentation and were not
  manually removed. The replacement rollback backup is therefore PASS; the
  legacy document's disposition remains explicit evidence for the later
  uninstall/removal-contract classification.
- The authorized reversible non-secret configuration change used only
  `qwsg config set`: `guardian.interval` changed from `5m0s` to `4m0s`.
  Effective identity changed from
  `sha256:a337c73f36345fbf9efd31763f3469e98a8e8e27e36339a6b069b844ae48763e`
  to
  `sha256:1ce5c953b84c0f2b4f751c85b8141339d67cc33c78a80cb7e7fd568205258dd2`.
  No restart or reboot accompanied the change.
- For the changed identity, notification preflight retained satisfied SMTP
  configuration and protected credential classifications, the single bounded
  controlled notification test was accepted by the configured SMTP server,
  and the Owner independently confirmed actual receipt. No protected
  notification identity, credential or message content was retained.
- The first preboot-record block stopped before creating its private record;
  no reboot, restart or additional configuration change occurred. Subsequent
  evidence shows the changed RC.6 identity naturally converged to satisfied
  canonical evidence and Guardian ready, while readiness truthfully reports
  notification external unknown and overall partial. Source inspection
  confirms the controlled `notification test` proves transport acceptance but
  does not publish a Guardian notification-queue entry; therefore the accepted
  test plus Owner-confirmed receipt is preserved as external notification PASS
  while the product readiness classification remains separately unknown. A
  duplicate message is not technically necessary merely to recreate the
  missing private preboot correlation record.
- A replacement private mode-0600 preboot correlation record was created on
  the VPS for the changed configuration identity, opaque boot/invocation/PID
  comparison values, accepted notification test, confirmed receipt, and the
  separately preserved unknown readiness classification. The record identity
  and mode passed; its opaque host values were not returned or retained in
  repository evidence.
- The single authorized physical reboot completed. Against the private record,
  boot identity, Guardian process and systemd invocation all changed. The
  exact packaged unit remained enabled/active with lingering. User manager,
  user default target and Guardian execution each preceded the interactive
  login; the returned `PASSPASS` strings concatenate two successful timestamp
  predicates and reconcile to PASS. `NRestarts=0`, so this boot did not
  naturally exercise `Restart=on-failure`; the one authorized bounded
  recovery injection remains available only after stable convergence proof.
- Post-reboot RC.6 convergence passed the QWSG-059-F001 boundaries: the active
  checkpoint matches the changed effective configuration and current systemd
  invocation, is active and has a completed cycle; Current Operator State is
  Guardian-running, current, complete and published for
  `guardian_observed`. Canonical evidence, service activity/enablement and
  Guardian readiness are satisfied. Product readiness remains overall partial
  solely because notification external is unknown despite the independently
  proven controlled delivery. This does not invalidate Guardian convergence,
  but remains a distinct limitation against the planned literal overall-ready
  result.
- Because the reboot produced no natural restart, one authorized recovery
  injection was performed only after the above stable Guardian-ready evidence
  and after a private mode-0700 on-VPS checkpoint/operator-state snapshot.
  A systemd-managed SIGKILL of the exact main service process occurred once.
  Packaged `Restart=on-failure` automatically recovered to a different
  invocation and process, the service returned active, and `NRestarts`
  incremented from 0 to 1. The injection will not be repeated.
- Recovery evidence shows the prior generation superseded, current
  invocation/configuration checkpoint ownership, active checkpoint, completed
  current-generation cycles, current Guardian-running state, canonical
  evidence satisfied and Guardian readiness ready. QWSG-059-F001 is therefore
  externally corrected in RC.6. Operator-state completeness and overall
  readiness remained partial through the bounded follow-up observation, with
  `notification.external=unknown_requires_verification`; that separate state
  must not be relabeled as canonical-evidence convergence failure.
- **QWSG-061-F001 — acceptance requirement adjustment, not product defect:**
  minimum read-only contract analysis confirms `qwsg notification test`
  directly performs one controlled provider delivery and reports acceptance,
  but intentionally writes no Guardian checkpoint/queue evidence. Readiness
  classifies notification satisfied only from an accepted/delivered entry in
  the Guardian's monitoring queue; an empty queue is configured-unverified.
  The product documentation explicitly permits Guardian core ready with
  notification/overall partial, says a controlled test verifies notification
  but is not configuration acceptance, and requires a test not create a
  monitoring incident. Thus Task 061's literal requirement that the
  independently confirmed test persist/reconcile into
  `notification.external=satisfied` and `overall=ready` exceeded the current
  product contract. Exact classification: **EXPECTED PRODUCT BEHAVIOR WITH
  ACCEPTANCE REQUIREMENT ADJUSTMENT**. The accepted SMTP test and confirmed
  receipt remain practical notification PASS; the product's unknown readiness
  remains truthful and separate.
- The Project Owner accepted that classification and corrected Task 061's
  acceptance contract accordingly: QWSG-059-F001 is externally corrected;
  no RC.6 product-source defect is established; controlled SMTP acceptance
  plus actual receipt passes practical notification; persistent
  `notification.external=satisfied` and literal `overall: ready` are not
  required on this current-contract path. Remaining previously authorized
  explicit-restart and lifecycle checks resume without new requirements.
- The Project Owner subsequently terminated all further VPS execution and made
  the latest semantic product state authoritative. No more restart, injection,
  notification, polling, uninstall/reinstall, diagnostic or state-inspection
  action occurred. A literal `restart.fresh_canonical_convergence=FAIL`
  predicate is reconciled as an acceptance-harness limitation: it depended on
  file timing/literal JSON assumptions while the product-owned evidence already
  proved current configuration and generation ownership, completed Guardian
  execution, canonical evidence satisfied, enabled/active service, Guardian
  ready and readiness exit zero. It cannot override those semantic contracts.
- Final external classification: physical reboot/pre-login autostart PASS;
  systemd automatic recovery PASS; current-generation/configuration ownership
  PASS; QWSG-059-F001 canonical convergence EXTERNALLY CORRECTED; controlled
  SMTP acceptance and actual receipt PASS; notification/overall partial
  EXPECTED; RC.6 product-source defects NONE. Explicit restart and external
  uninstall/same-candidate reinstall are NOT EXECUTED BY OWNER TERMINATION and
  remain disclosed non-product evidence limitations.
- Added `docs/release/ACCEPTANCE_1.1.0-rc.6.md` with the frozen candidate
  identities, additive external evidence, contract correction, harness
  reconciliation, limitations and `READY FOR RELEASE DECISION` technical
  recommendation. This is not release publication or final release action.
- Final repository validation passed: Framework 1.1.0, 24 framework
  assertions, 36 diversion assertions, 29 lifecycle assertions, 44 Builder
  assertions, active-job and diverted-test audit, release plumbing,
  checkout/export build provenance, full `go test ./...`, repository-wide
  race tests, `go vet ./...`, Go formatting and Git whitespace. No product
  source changed in Task 061; the frozen candidate identities remain
  immutable, and the unrelated Owner blueprint remained excluded and
  untouched.

## Rollback

- Before selection, isolated failed/missing build harness state was bounded to
  `/tmp`. After selection the frozen candidate is retained immutably and must
  not be rebuilt or substituted.
- Repository rollback is limited to literal Task 061 lifecycle/evidence paths
  from the verified execution snapshot. No broad reset/checkout/clean or Owner
  content access is permitted.

## Completion state

`complete — targeted practical acceptance PASS; QWSG-059-F001 externally corrected; rc.6 READY FOR RELEASE DECISION; explicit publication remains Owner-reserved`
