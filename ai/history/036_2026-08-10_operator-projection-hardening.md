# Task History 036: Approved Task

## Task metadata

- Task ID: `036`
- Task slug: `operator-projection-hardening`
- Status: `complete`
- Date generated: `2026-08-10` UTC
- Human authority: Project Owner
- Preferred owner communication language: Hungarian
- Related prompt: `ai/prompts/036_CURRENT_TASK.md`

## Lifecycle state

The Engineering Task Builder generated and transactionally installed this matching prompt/history pair from validated structured owner input. Task execution started through the canonical `job` workflow after complete prompt, history, skill, governance, Framework, lifecycle, repository, and Git validation.

## Starting state

- Repository `/home/qws/web/qwsg.quantumwizard.hu/qwsg`, branch `main`, HEAD `0a8a5c7e722495b8c5eb425bca5b2d2413aaa175`; canonical HTTPS origin and `0/0` relationship verified.
- Task 036 is the sole active approved task, Task 035 is the latest completed baseline, the Git index is empty, and all pre-existing Owner-owned modified and untracked content was inventoried for preservation.
- Framework, lifecycle, diverted-task, focused Presentation Model/Console/Current State/CLI tests, focused vet, complete Go formatting, and Git diff checks passed.
- Repository and read-only live-store evidence confirmed the narrow boundary: all canonical evaluation stages complete with sufficient evidence; the Presentation Model creates 366 matched Rule plus 366 observe Policy attention candidates, exceeds `MaxAttention = 256`, returns `invalid operator overview`, and the CLI collapses that category to `evaluation_failed` before Current Operator State publication.
- Release Minimalism decision: fix only existing Presentation Model reduction, application diagnostics, recommendation semantics, Console disclosure, compatibility, tests, and directly affected documentation. Canonical engine semantics remain frozen.

## Snapshot

Verified implementation snapshot: `/tmp/qwsg-task036-implementation-20260810TTc3nNb`. It contains 27 existing exact target payloads, two absence records, repository/Git identity, permissions, ACLs, checksums, and bounded restore instructions. All 36 manifest entries passed SHA-256 verification before implementation.

## Work performed

Implementation contract frozen before code changes:

- Rule/Policy attention correlation uses only validated `PolicyEvaluation.RuleEvaluationID` identity; the downstream Policy concern is authoritative and the correlated Rule view is counted as aggregated.
- Remaining candidates are globally sorted by severity, operator importance, stable tokens, full source identity, and observation time before retaining at most `MaxAttention`; input enumeration cannot displace a later urgent concern.
- Overview overflow accounting records total pre-correlation candidates, represented candidates, correlated duplicates, and omitted candidates; `total = represented + correlated + omitted`.
- Compatibility analysis proved that an additive field would be rejected by existing strict decoders. Presentation Model and Current Operator State therefore receive the smallest coherent `1.1` version bump, while new readers explicitly accept coherent legacy State/Overview `1.0` records without a reduction summary.
- Pipeline execution, projection, and Current State publication errors receive typed application-boundary categories and fixed privacy-safe terminal tokens; no raw error is exposed.

Implemented behavior:

- Presentation collects contract-bounded candidates, removes a Rule view only when an exact validated Policy `RuleEvaluationID` proves downstream correlation, globally ranks severity and evidence importance before truncation, and retains at most 256 items.
- `AttentionSummary` records total, represented, correlated, and omitted counts under a validated bounded invariant and participates in canonical identity/JSON. Console Home and Attention views localize the explicit non-exhaustive disclosure.
- Direct Health/Alert/Runtime/Guardian facts outrank final Policy decisions and intermediate Rule views at equal severity; complete source references remain on every represented item.
- `observe` uses typed `evaluation_pipeline_failed`, `operator_projection_failed`, and `current_state_publication_failed` categories while preserving specific safe store/state diagnostics.
- Missing/stale evidence retains `run_fresh_check`; current partial/unsupported evidence now uses `inspect_evidence`; explicit failed operations retain inspection rather than blind retry.
- No Inventory, Comparison, Drift, Health, Rule, Policy, Report, Command, Pipeline, Runtime, or Runtime Service semantics changed.

## Verification

Validation evidence:

- Focused Presentation Model, Current State, Console, and CLI tests pass. The realistic fixture runs 367 canonical changes through Drift, Health, Rule, and Policy; it creates more than 732 attention candidates, shuffles equivalent inputs, retains a late urgent direct fact, validates bounded traceability and reduction counts, publishes Current State, and is consumed by a separate bare-`qwsg` subprocess.
- Full `go test ./...`, repository-wide `go test -race ./...`, `go vet ./...`, `make build`, `make test`, `make vet`, `make fmt-check`, `git diff --check`, Framework validation and 21 assertions, Builder 38 assertions, lifecycle 28 assertions, and diversion 36 assertions pass with isolated writable caches.
- The first post-documentation Go invocation encountered only the environment's read-only default `/home/attila/.cache/go-build`; rerunning the identical matrix with `/tmp/qwsg-task036-go-cache` passed. An earlier test-fixture clock placed qualified evidence after its injected publication time; correcting the fixture clock exposed no production defect and the final tests pass.
- A real collector smoke test used only `/tmp/qwsg-task036-live.1RKqaG`: baseline publication and separate-process Console loading succeeded. This execution environment reported partial Inventory, so the subsequent full profile correctly failed closed as `operator_projection_failed` at incomplete stage coverage and preserved the limited prior state. It cannot satisfy the prompt's complete-evidence live acceptance, which is instead exercised deterministically by the required subprocess fixture; no real user state was read or modified.
- Documentation now records Model/State 1.1 compatibility, prioritization, exact correlation, explicit reduction disclosure, differentiated diagnostics, and truthful recommendations in architecture, product, specification, README, English/Hungarian user guidance, System Map, Roadmap, and Engineering History.
- The Git index remains empty. Pre-existing Owner-owned modifications and untracked files remain present; only Task 036 bounded targets were changed by this execution. No installation, network operation, dependency, staging, commit, push, package, deployment, or release occurred.

Implementation and verification gates are complete. The matching prompt is ready for exact archival without a successor.

## Rollback

Use only the verified bounded snapshot after confirming repository identity, exact target identity, manifest checksums, and absence of later Owner edits. Restore exact payload targets only; remove a created Task 036 archive only when its pre-change absence and exact Task identity are proven. Broad Git recovery and real user-state deletion are forbidden.

## Completion state

`complete`

The completed prompt is archived at
`ai/archive_prompts/036_2026-08-10_operator-projection-hardening.md` without a
successor. Final `bin/job --check` reports canonical idle with Task 036 as the
unique latest completed baseline; Framework, lifecycle, diverted-task, diff,
empty-index, branch/HEAD, and `origin/main...main = 0/0` checks pass.
