# Task History 055: Approved Task

## Task metadata

- Task ID: `055`
- Task slug: `qwsg-1-1-0-rc-4-clean-host-acceptance-release-readiness`
- Status: `complete with disclosed limitations — NOT READY FOR QWSG 1.1.0 RELEASE`
- Date generated: `2026-08-24` UTC
- Human authority: Project Owner
- Preferred owner communication language: English
- Related prompt: `ai/prompts/055_CURRENT_TASK.md`

## Lifecycle state

The Engineering Task Builder generated and transactionally installed this matching prompt/history pair from validated structured owner input. Explicit approval was recorded. The Project Owner started Task 055 and authorized Gate A on 2026-08-24 UTC. Gate A acceptance scaffolding was integrated and pushed as `f105771dbddccf363a63095ac4ad2a7a2285aa84`. The Owner then authorized Gate B private deterministic twin construction. The first attempt stopped on an acceptance-orchestration failure. Read-only diagnosis proved the ordinary exported build passed and the direct release-script invocation used the wrong working directory. One separately Owner-authorized corrected module-root retry then passed.

## Starting state

`HEAD`, `origin/main` and direct Forgejo `main` were
`12069b16cc574c759a40f905d2b4981bd729716d`, ahead/behind `0/0`, branch
`main`, index and tracked tree clean. Task 055 was the sole active task; its
Builder-created prompt/history and the excluded Owner-owned blueprint were the
only untracked paths. `VERSION` was `1.1.0-rc.4`; Task 054 integration commit
`ef513dde187e4119b6aa04a3439a879056f6cc69` was an ancestor. No RC.4
candidate, sidecar, tag, release, transfer or external acceptance existed.

## Snapshot

Private mode-0700 Gate A snapshot:
`/tmp/qwsg-task055-gate-a.v40UtE`. It contains a readable exact tracked-HEAD
archive, Task 055 lifecycle records, RC.3 protocol/ledger, release-plumbing
baseline and bounded target evidence. Candidate bytes, credentials, private
external identity and Owner content are excluded.

## Work performed

- Audited the exact integrated RC.4 source, release builder, package/document
  surfaces and prior 25-checkpoint acceptance model.
- Prepared an RC.4-specific 25-checkpoint Owner-operated protocol with explicit
  Gates A–F and fresh `QWSG-053-F001`, `QWSG-051-F001`, F002/F003 boundaries.
- Prepared an empty RC.4 evidence ledger: candidate NOT BUILT, transfer and
  external acceptance NOT STARTED, verdict PENDING, all checkpoints NOT
  EXECUTED, all receipts NOT CONFIRMED.
- Extended release-plumbing validation only because the new RC.4 protocol and
  ledger require structural and preservation assertions.
- No candidate construction, transfer, external or credential action occurred.

## Verification

Builder/lifecycle identity, Task 054 ancestry, ordinary checkout/export build
contract, release validate-only plumbing, 25-checkpoint protocol structure,
full local/governance/security/exclusion checks and protected RC.1/RC.2/failed
RC.3/F001/F002/F003/v1.0.0/LICENSE hashes passed. Exact results are retained in
the Gate A Owner report. No external evidence was inferred.

## Gate B construction attempt

- Owner-authorized source: `f105771dbddccf363a63095ac4ad2a7a2285aa84`.
- Derived `SOURCE_DATE_EPOCH`: `1787571815`; UTC identity:
  `2026-08-24T11:43:35Z`.
- Verified `HEAD`, `origin/main`, and direct Forgejo `main` at the authorized
  source, ahead/behind `0/0`, with Task 055 as the sole active task.
- Created and verified a private mode-0700 rollback snapshot outside Git.
- Created two independent private mode-0700 build roots and independently
  exported the exact commit into genuine source trees without `.git` metadata.
- The first export's ordinary no-`.git` `make build` with `GOFLAGS` unset
  succeeded. The subsequent release script was invoked directly while the
  process working directory remained the canonical checkout. Go rejected the
  exported absolute `cmd/qwsg` path as outside the selected module.
- Fail-safe result: stopped immediately without a second build, release
  candidate, workaround or product change. Read-only diagnosis classified the
  event as an acceptance-procedure/orchestration defect, not a product defect.
- The original roots and evidence remain preserved unchanged.

## Gate B corrected retry

- Owner authorized exactly one corrected retry from two new independent
  private mode-0700 roots. A separate verified private retry snapshot remains
  outside Git.
- Both exact-commit exports contained no `.git`; `GOFLAGS` was unset. Ordinary
  builds used `make -C <export-root> build`; release builds used
  `make -C <export-root> release` with full commit, commit-derived epoch and
  independent output/cache roots.
- Both ordinary builds passed with truthful `commit: unknown` and
  `built: unknown`. Both release constructions passed with exact version
  `1.1.0-rc.4`, commit `f105771dbddccf363a63095ac4ad2a7a2285aa84`,
  and UTC build identity `2026-08-24T11:43:35Z`.
- The binaries, manifests, archives and sidecars were byte-identical. Archive:
  `2949478` bytes, SHA-256
  `7ad8a0f1be9bdbaa4403c3a816a6b474fd8e052934abd031047e4f82fe73a333`.
  Sidecar SHA-256:
  `ca4daca22dd9f6accf5cdb062c3bb7aa88066f70b3988ae8fb1f189875d954bc`.
  Manifest SHA-256:
  `ac1e31000e85ef406249675dc9dbdcda53ea848c571c7e4aeba4a3a4f72ad588`.
  Binary SHA-256:
  `3ec977090f90ec21a535e0a5c4fe158ab6eee883af4f230c6423514dd1cba92b`.
- Sidecars and both internal manifests verified. Static Linux amd64 identity,
  one safe canonical archive root, exact expected regular-file/directory set,
  deterministic ownership/time/modes, README, INSTALL, LICENSE, RC.4 release
  notes and operator documentation passed. Package secret/private-key/token,
  lifecycle/Builder, snapshot/cache/backup, `.git`, Owner-content and prior-RC
  exclusions passed.
- A newline-quoting error in the first identity-validation shell assertion was
  diagnosed without rebuilding; the same candidate bytes passed the corrected
  exact multiline identity check. This was validation orchestration, not a
  candidate or product failure.
- Bounded `QWSG-053-F001` status: local Gate B retest PASS. The immutable
  historical finding remains `OPEN, BLOCKING`; no external correction or final
  release readiness is claimed.

## Gate C evidence integration

The Project Owner accepted the deterministic candidate proof and authorized
integration of only privacy-safe Gate B chronology, identities and validation
results. Private build-root identities, host identities, credentials and
private evidence payloads are excluded. No candidate rebuild, transfer,
external execution or external acceptance occurred.

## RC.4 external acceptance finding

Gate D used the separately authorized Owner-workstation fallback. Exactly the
verified build-one archive and sidecar were privately transferred. Destination
evidence proved two regular non-symlink files, exact size and digests, and a
passing sidecar check without retaining credentials, private host identity or
private paths. External Checkpoints 01–09 then passed, including the fresh
Smart Install F002/F003 regression and immutable installation. Configuration
was written safely before the guided activation boundary. No SMTP credential or
receipt checkpoint was performed.

The Owner followed only the documented guided RC.4 workflow. Guardian
activation successfully reached the validated user manager, completed
`daemon-reload`, and completed `enable --now`. The unit was enabled and its
configuration condition passed, but the Guardian exited with
`guardian_state_unsafe`. `ExecStopPost` reported unavailable exit-checkpoint
evidence. `Restart=on-failure` made three restart attempts before the start
limit, leaving the service failed and inactive without fresh canonical
Guardian evidence.

Privacy-safe bounded diagnostics established that systemd created a
compatibility symlink at the requested user state directory because that state
directory was absent while the same-name QWSG configuration directory already
existed. This is systemd's user-service migration behavior for layouts from
systemd 253 or older. The packaged `StateDirectory=qwsg` contract therefore
collides with setup's existing same-name configuration directory. The unit's
`WorkingDirectory`, `QWSG_STATE_DIR`, and writable-path directives route the
Guardian to that symlink, while QWSG correctly rejects symlinked state roots.

Finding `QWSG-055-F001` is an `OPEN, BLOCKING` `RELEASE BLOCKER`: packaged
systemd user-unit/state-storage integration is incompatible with QWSG's
state-path security contract. The compatibility symlink also explains the
post-install `filesystem.local_semantics` regression from satisfied absent-path
evidence to unsafe-path uncertainty. External acceptance stopped. No symlink,
permission, unit or service state was manually changed; no workaround and no
later SMTP, reboot, uninstall or reinstall checkpoint was attempted.

## Rollback

After Owner authorization, restore only the exact Gate A paths from the private
snapshot and remove only RC.4 files recorded absent. Never reset/clean broadly,
alter historical evidence, candidate bytes, tags, remote state or Owner content.

## Completion state

`complete with disclosed limitations — NOT READY FOR QWSG 1.1.0 RELEASE`.
QWSG-055-F001 remains `OPEN, BLOCKING`; Checkpoints 11–13 and 17–25 remain
unexecuted. A separately authorized correction task, new candidate identity,
and clean-host acceptance restart from Checkpoint 01 are required.

The privacy-safe blocker evidence was integrated as commit
`af70281a2694feb23ffeed5671ee7da173c5287e` with subject
`docs: record RC.4 clean-host acceptance blocker`. Under separate Owner
authority, the completed-with-limitations prompt was archived and the
repository returned to canonical idle. The closure snapshot is
`/tmp/qwsg-task055-closure.t5IuF9`; rollback is bounded to the Task 055 prompt,
archive destination and this history record.
